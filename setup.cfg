[metadata]
name = crispy-goggles
version = 0.0.0
description = Playwright test helpers (pytest + playwright)

[options]
packages = find:
# Dependencies are declared in `pyproject.toml` and installed via `pip install -e '.[dev]'` or `make setup`.

[options.packages.find]
where = .
exclude = tests
